The script [main.py](main.py) describes how to simulate training and validation data, 
encode them into summary statistics, 
train a DL-based model on these data, 
and then apply the trained model to the simulated trees.